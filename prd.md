# 屏幕录制器 PRD - macOS 菜单栏应用

## 概述
一个轻量级、用户友好的屏幕录制应用，位于 macOS 菜单栏中，类似于原生屏幕录制工具，但用于视频录制。

## 核心功能

### 1. 屏幕录制
- 录制整个屏幕或用户自定义区域
- 区域选择类似于 macOS 截图工具（拖拽选择）
- 区域选择后需要有一个确认按钮，来确认并退出
- 只支持单显示器
- 最高质量录制（原生屏幕分辨率）--这个需要仔细考虑保证，考虑使用ScreenCaptureKit
- 录制期间录制区域周围显示绿色边框高亮（但边框不能出现在录制视频中）

### 2. 音频录制
- 录制麦克风输入
- 录制系统音频
- 两个音频源可以独立启用/禁用

### 3. 摄像头叠加层（核心功能）
- 可选在屏幕上不同位置放置的圆形摄像头叠加层
- 用户可在录制期间可随时调整叠加层大小和位置 （左上、右上、左下、右下，以及3种尺寸选择）
- 白色窄边框
- 始终使用最高摄像头分辨率
- 摄像头画面嵌入最终视频输出

### 4. 录制控制
- 从菜单栏开始/停止录制
- 显示录制状态的视觉指示器
- 无需键盘快捷键
- 无需倒计时器

### 5. 视频输出
- 保存为 MP4 文件（H.264 编码）
- 默认保存位置：桌面
- 用户可以更改保存位置
- 组合输出包括屏幕录制 + 摄像头叠加层，保存在单个文件中

## 技术要求
- **平台**：macOS（针对 Apple Silicon M 系列芯片优化）
- **语言**：Swift
- **应用类型**：菜单栏应用程序
- **分发方式**：直接下载（非 Mac App Store）
- **最低 macOS**：兼容所有 M 系列 Mac

## 设计原则
- 符合 Apple 设计语言的简单和极简 UI
- 轻量级和快速性能
- 用户友好，学习曲线最小
- 无不必要的功能或复杂性

## 不在范围内
- 窗口特定录制
- 录制预设
- 录制后编辑功能（修剪、剪切）
- 键盘快捷键
- 倒计时器
- 摄像头叠加层的视觉效果

## 测试和调试，先用命令行调试（有日志），用户确认后再打包为app